items:
  - name: NoFieldRemoval
    whyItMatters: If fields are removed, then clients that rely on those fields will not
      be able to read them or write them.
    errors:
    - crd/schedulers.config.openshift.io version/v1 enum/"NoScoring" may not be removed
      for field/^.spec.profile
    warnings:
    infos:
